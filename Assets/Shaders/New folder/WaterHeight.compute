#pragma kernal CSMain


//StructuredBuffer<float> heights;
//float heights[1000];
//StructuredBuffer<float2> positions;

//[numthreads(8,8,1)]
void FunctionKernel (uint id : SV_GROUPTHREADID)
{
	heights[id] = positions[id].x + positions[id].y;
	
}
